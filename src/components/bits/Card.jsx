import React from 'react';
import styled from 'styled-components/macro';

const Container = styled.div`

`;

const Card = () => (
  <Container>
    <h1>ciao</h1>
  </Container>
);

export default Card;
